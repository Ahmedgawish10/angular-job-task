import { ManDirective } from './man.directive';

describe('ManDirective', () => {
  it('should create an instance', () => {
    const directive = new ManDirective();
    expect(directive).toBeTruthy();
  });
});
